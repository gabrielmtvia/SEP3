syntax = "proto3";

option java_package = "via.sep3.grpc.order";

message OrderMessage {
  string order = 1;
}

message VoidMessage {

}

service OrderService {
  rpc getAllOrders(VoidMessage) returns (OrderMessage);
  rpc createOrder(OrderMessage) returns (VoidMessage);
}